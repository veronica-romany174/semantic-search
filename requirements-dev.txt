# ── Test / dev dependencies ────────────────────────────────────────────────────
# Install with: pip install -r requirements-dev.txt
# (Production deps are in requirements.txt)

pytest==8.3.5
pytest-asyncio==0.24.0
httpx==0.28.1          # required by TestClient (Starlette 0.52+)
